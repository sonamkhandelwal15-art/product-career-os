{
  "name": "product-career-os",
  "version": "1.0.0",
  "description": "52-week product career build: curriculum, case vault, AI artifact review on Claude, portfolio and interview prep.",
  "scripts": {
    "start": "node --no-warnings server.js",
    "dev": "node --no-warnings --watch server.js",
    "mock": "node --no-warnings -e \"process.env.MOCK_AI='1'; import('./server.js')\"",
    "doctor": "node --no-warnings doctor.js",
    "test": "node --no-warnings test/env.test.js && node --no-warnings test/auth.test.js && node --no-warnings test/workspace.test.js && node --no-warnings test/doctor.test.js && node --no-warnings test/api.test.js && node --no-warnings test/ui.test.js"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.122.0",
    "express": "^5.2.1",
    "mammoth": "^1.12.2",
    "multer": "^2.3.0",
    "pdf-parse": "^2.4.5"
  },
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.5"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}